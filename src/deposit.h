#ifndef DEPOSIT_H
#define DEPOSIT_H
#include <stddef.h>

void vapod (int aod, int pod);						// Функция для проверки на коректность введенных данных
void income_deposit(int aod, int pod);				//Функция расчета суммы вклада на момент окончания срока

#endif
